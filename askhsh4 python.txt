print "first min is" , min(list)
print "first max is" , max(list)
list.remove(min(list))
list.remove(max(list))
print "second min is", min(list)
print "second max is" , max(list)
i=len(list)
j=0
sum=0
for(j=0, j<=i , j++)
{
sum=sum + list(j)
}
Sum=sum/i
print math.sqrt(sum)
// Αρχικά θα εμφανίσω τον μεγαλύτερο και μικρότερο αριθμό της λίστας και στην συνέχεια θα τους αφαιρέσω από αυτή. Αμέσως μετά θα ακολουθήσω την ίδια διαδικασία και για τον μεγαλύτερο και μικρότερο αριθμό που υπάρχουν στην ανανεωμένη λίστα. Στην συνέχεια θα εκχωρήσω στην μεταβλητή i το μήκος της λίστας με την βοήθεια της αντίστοιχης συνάρτησης. Θα χρησιμοποιήσω μια επαναληπτική διαδικασία η όποια θα υπολογίζει με την βοήθεια της μεταβλητής sum το άθροισμα των στοιχείων της λίστας.
Αμέσως μετά το πέρας της διαδικασίας αυτής η μεταβλητή sum θα έχει ως τιμή τον μέσο όρο των στοιχείων της λίστας. Τέλος με την βοήθεια της αντίστοιχης μαθηματικής συνάρτησης θα εμφανίσω την τετραγωνική ρίζα του μέσου όρου αυτού.
